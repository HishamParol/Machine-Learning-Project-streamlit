# Covid dataset. Currently not using it
# import pandas as pd


# data = pd.read_csv('../xlsx-csv/time_series_covid19_confirmed_global.csv')
# data = data.drop(['Province/State', 'Country/Region', 'Lat', 'Long'], axis=1)

# data2 = data.T
# print(data2.head(6))
# percentageChange = data2.pct_change();
# print("Percentage Change for Covid19: (Each column representing each Region");
# print(percentageChange);
